---
phase: 29-stripe-billing
plan: 03
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - app/policies/api_key_policy.rb
  - app/controllers/settings/api_keys_controller.rb
  - app/models/concerns/plan_limits.rb
  - app/controllers/symptom_logs_controller.rb
  - app/controllers/peak_flow_readings_controller.rb
  - app/views/symptom_logs/index.html.erb
  - app/views/peak_flow_readings/index.html.erb
  - test/models/plan_limits_test.rb
  - test/controllers/settings/api_keys_controller_test.rb
autonomous: true

must_haves:
  truths:
    - "Free users cannot generate API keys — the API key page shows an upgrade prompt instead of the generate button"
    - "Free users see only the last 30 days of symptom logs and peak flow readings"
    - "Premium users see unlimited history and can generate API keys"
    - "When a premium user downgrades, they see an upgrade prompt on gated features (not an error page)"
    - "Admins retain full access to everything regardless of subscription"
  artifacts:
    - path: "app/policies/api_key_policy.rb"
      provides: "API key policy gated by premium status"
      contains: "premium?"
    - path: "test/models/plan_limits_test.rb"
      provides: "Tests for history scoping by plan"
      contains: "class PlanLimitsTest"
  key_links:
    - from: "app/policies/api_key_policy.rb"
      to: "app/models/concerns/plan_limits.rb"
      via: "user.premium?"
      pattern: "user\\.premium\\?"
    - from: "app/controllers/symptom_logs_controller.rb"
      to: "app/models/concerns/plan_limits.rb"
      via: "plan_features[:symptom_log_history_days]"
      pattern: "plan_features|history_days"
---

<objective>
Gate premium features (API keys, unlimited history) behind subscription status using Pundit policies and plan-aware query scoping, with graceful upgrade prompts for free users.

Purpose: Implements BILL-01 (free users have feature limits) and BILL-05 (premium features gated by plan, downgrade shows upgrade prompt not error).
Output: API keys gated behind premium, history limited for free users, upgrade prompts shown, tests passing.
</objective>

<execution_context>
@~/.claude/ariadna/workflows/execute-plan.md
@~/.claude/ariadna/templates/summary.md
</execution_context>

<context>
@.ariadna_planning/PROJECT.md
@.ariadna_planning/ROADMAP.md
@.ariadna_planning/phases/29-stripe-billing/29-RESEARCH.md
@.ariadna_planning/phases/29-stripe-billing/29-01-SUMMARY.md
@app/policies/settings_policy.rb
@app/controllers/settings/api_keys_controller.rb
@app/controllers/symptom_logs_controller.rb
@app/controllers/peak_flow_readings_controller.rb
@app/views/symptom_logs/index.html.erb
@app/views/peak_flow_readings/index.html.erb
@app/models/concerns/plan_limits.rb
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate API keys behind premium subscription and add upgrade prompt</name>
  <files>
    app/policies/api_key_policy.rb
    app/controllers/settings/api_keys_controller.rb
    app/views/settings/api_keys/show.html.erb
    test/controllers/settings/api_keys_controller_test.rb
  </files>
  <action>
    1. Create `app/policies/api_key_policy.rb` (headless policy — the API key controller currently uses `authorize :settings, :show?` which allows all users):
       ```ruby
       class ApiKeyPolicy < ApplicationPolicy
         def show?
           true  # All users can see the API key page (free users see upgrade prompt)
         end

         def create?
           user.premium?  # Only premium users can generate keys
         end

         def destroy?
           user.premium?  # Only premium users can revoke keys (they had to be premium to create one)
         end
       end
       ```

    2. Update `app/controllers/settings/api_keys_controller.rb`:
       - Change the `before_action` from `authorize :settings, :show?` to action-specific authorization
       - In `show`: `authorize :api_key` (uses ApiKeyPolicy#show?, which allows everyone)
       - In `create`: `authorize :api_key, :create?` (Pundit will deny free users)
       - In `destroy`: `authorize :api_key, :destroy?`
       - Rescue from `Pundit::NotAuthorizedError` is already handled in ApplicationController (redirects with flash), but for a better UX on the API key page, the show view should check `Current.user.premium?` and conditionally show either the key management UI or an upgrade prompt.

    3. Update `app/views/settings/api_keys/show.html.erb`:
       - Read the existing view first to understand its structure
       - Wrap the "Generate API Key" button / key management section in a `<% if Current.user.premium? %>` conditional
       - In the `<% else %>` branch, show an upgrade prompt card explaining that API keys are a premium feature, with a link to `settings_billing_path` ("Upgrade to Premium")
       - Style the upgrade prompt using the existing design system (`.section-card`, `.btn--primary`)
       - Keep the page header and overall layout the same regardless of plan

    4. Update or create tests in `test/controllers/settings/api_keys_controller_test.rb`:
       - Test that GET /settings/api_key returns 200 for both free and premium users
       - Test that POST /settings/api_key (create) redirects with error for free users (Pundit denies)
       - Test that POST /settings/api_key (create) works for premium users (or admin users, since admin? implies premium?)
       - If the existing test file already tests API key creation, ensure those tests set up the user as premium (admin fixture should work since admins are always premium)

    5. Run `bin/rails test test/controllers/settings/api_keys_controller_test.rb` and fix failures.
    6. Run `bin/rubocop` on modified files.
  </action>
  <verify>
    - `bin/rails test test/controllers/settings/api_keys_controller_test.rb` passes
    - Free user accessing /settings/api_key sees upgrade prompt (not generate button)
    - Free user POSTing to create API key gets denied by Pundit (redirect with error)
    - Admin/premium user can still generate and manage API keys
  </verify>
  <done>API keys are gated behind premium subscription. Free users see an upgrade prompt with link to billing page. Pundit policy enforces create/destroy for premium users only. Tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Limit history views for free users and add upgrade banner</name>
  <files>
    app/models/concerns/plan_limits.rb
    app/controllers/symptom_logs_controller.rb
    app/controllers/peak_flow_readings_controller.rb
    app/views/symptom_logs/index.html.erb
    app/views/peak_flow_readings/index.html.erb
    test/models/plan_limits_test.rb
  </files>
  <action>
    1. Add a helper method to `app/models/concerns/plan_limits.rb`:
       ```ruby
       def history_cutoff_date(feature_key)
         days = plan_features[feature_key]
         return nil if days.nil?  # nil means unlimited
         days.days.ago.beginning_of_day
       end
       ```
       This returns nil for premium (unlimited) or a date for free users.

    2. Update `app/controllers/symptom_logs_controller.rb` — in the `index` action, scope the query by plan limits:
       - Read the existing controller to find the current query pattern
       - After the existing query, add date scoping for free users:
         ```ruby
         cutoff = Current.user.history_cutoff_date(:symptom_log_history_days)
         @symptom_logs = @symptom_logs.where("recorded_at >= ?", cutoff) if cutoff
         ```
       - Store `@history_limited = cutoff.present?` for use in the view
       - Do NOT change anything else about the controller. Just add the scope.

    3. Update `app/controllers/peak_flow_readings_controller.rb` — same pattern as symptom logs:
       - Scope by `history_cutoff_date(:peak_flow_history_days)` in the `index` action
       - Store `@history_limited` flag

    4. Update `app/views/symptom_logs/index.html.erb`:
       - Read the existing view first
       - Add a subtle upgrade banner near the top of the content (after page header, before the list) when `@history_limited` is true:
         ```erb
         <% if @history_limited %>
           <div class="section-card" style="border-left: 3px solid var(--brand); margin-bottom: 1.5rem;">
             <p style="margin: 0;">
               <strong>Free plan:</strong> Showing the last 30 days.
               <%= link_to "Upgrade to Premium", settings_billing_path %> for unlimited history.
             </p>
           </div>
         <% end %>
         ```
       - Use existing design system classes. Keep it subtle — informational, not aggressive.

    5. Update `app/views/peak_flow_readings/index.html.erb` — same banner pattern.

    6. Create `test/models/plan_limits_test.rb` to test the concern in isolation:
       - Test `history_cutoff_date` returns nil for premium users (unlimited)
       - Test `history_cutoff_date` returns a date 30 days ago for free users
       - Test that the date is beginning_of_day (not current time)

    7. Run `bin/rails test test/models/plan_limits_test.rb`.
    8. Run `bin/rails test` for full suite.
    9. Run `bin/rubocop` on all modified files.
  </action>
  <verify>
    - `bin/rails test test/models/plan_limits_test.rb` passes
    - `bin/rails test` passes (no regressions)
    - `bin/rubocop` clean on modified files
    - Free users see "Showing last 30 days" banner on symptom logs and peak flow readings
    - Premium/admin users see full history with no banner
  </verify>
  <done>Free users see only 30 days of symptom log and peak flow reading history with an upgrade banner. Premium users see unlimited history. History scoping is plan-aware via PlanLimits concern. Tests pass.</done>
</task>

</tasks>

<verification>
- Full test suite passes: `bin/rails test`
- Free user at /settings/api_key sees upgrade prompt, not generate button
- Free user at /symptom-logs sees 30-day history with upgrade banner
- Free user at /peak-flow-readings sees 30-day history with upgrade banner
- Premium user sees full history, no banners, full API key access
- Admin user has full access to everything
- Pundit policies correctly gate premium features
</verification>

<success_criteria>
- API key generation/revocation gated behind premium subscription via Pundit policy
- Free users see upgrade prompts (not error pages) when accessing gated features
- Symptom log and peak flow reading history limited to 30 days for free users
- Upgrade banners link to billing settings page
- Premium and admin users have unrestricted access
- All tests pass with no regressions
</success_criteria>

<output>
After completion, create `.ariadna_planning/phases/29-stripe-billing/29-03-SUMMARY.md`
</output>
