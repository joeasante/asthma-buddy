---
phase: 02-authentication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models/user.rb
  - app/models/session.rb
  - app/models/current.rb
  - app/controllers/sessions_controller.rb
  - app/controllers/registrations_controller.rb
  - app/controllers/concerns/authentication.rb
  - app/views/sessions/new.html.erb
  - app/views/registrations/new.html.erb
  - config/routes.rb
  - db/migrate/xxx_create_users.rb
  - db/migrate/xxx_create_sessions.rb
  - test/models/user_test.rb
  - test/fixtures/users.yml
  - test/fixtures/sessions.yml
  - test/controllers/registrations_controller_test.rb
  - test/controllers/sessions_controller_test.rb
autonomous: true

must_haves:
  truths:
    - "A visitor can fill in a signup form with email and password and create an account"
    - "A registered user can log in with correct credentials"
    - "A user who enters wrong credentials sees an error and is not logged in"
    - "User emails are unique — duplicate signup is rejected"
  artifacts:
    - path: "app/models/user.rb"
      provides: "User model with has_secure_password and email validation"
      contains: "has_secure_password"
    - path: "app/models/session.rb"
      provides: "Session model belonging to user"
      contains: "belongs_to :user"
    - path: "app/controllers/registrations_controller.rb"
      provides: "Signup form and user creation"
      exports: ["new", "create"]
    - path: "app/controllers/sessions_controller.rb"
      provides: "Login and logout"
      exports: ["new", "create", "destroy"]
    - path: "app/views/registrations/new.html.erb"
      provides: "Signup form"
    - path: "app/views/sessions/new.html.erb"
      provides: "Login form"
    - path: "test/fixtures/users.yml"
      provides: "Test user fixtures with password_digest"
  key_links:
    - from: "app/views/registrations/new.html.erb"
      to: "RegistrationsController#create"
      via: "form POST"
      pattern: "form.*registrations"
    - from: "app/controllers/sessions_controller.rb"
      to: "User.authenticate_by"
      via: "authentication lookup"
      pattern: "authenticate_by"
    - from: "app/controllers/concerns/authentication.rb"
      to: "Current.session"
      via: "session lookup from cookie"
      pattern: "Current\\.session"
---

<objective>
Generate the Rails 8 authentication scaffold, add a RegistrationsController for user signup, configure the User model with email uniqueness and password validation, and establish the session-based login/logout flow.

Purpose: Provides the authentication backbone (AUTH-01 partial, AUTH-03 partial) that all subsequent auth plans build on — user creation, credential verification, and session management.
Output: Working signup and login/logout forms backed by User and Session models, with model tests, fixtures, and integration tests.
</objective>

<execution_context>
@~/.claude/ariadna/workflows/execute-plan.md
@~/.claude/ariadna/templates/summary.md
</execution_context>

<context>
@.ariadna_planning/PROJECT.md
@.ariadna_planning/ROADMAP.md
@.ariadna_planning/STATE.md
@app/views/layouts/application.html.erb
@app/controllers/application_controller.rb
@config/routes.rb
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate auth scaffold, add RegistrationsController, configure User model</name>
  <files>
    app/models/user.rb
    app/models/session.rb
    app/models/current.rb
    app/controllers/sessions_controller.rb
    app/controllers/registrations_controller.rb
    app/controllers/concerns/authentication.rb
    app/views/sessions/new.html.erb
    app/views/registrations/new.html.erb
    config/routes.rb
    db/migrate/xxx_create_users.rb
    db/migrate/xxx_create_sessions.rb
    db/schema.rb
  </files>
  <action>
    1. Run `bin/rails generate authentication` to scaffold the auth system. This generates:
       - User model with has_secure_password
       - Session model (belongs_to :user)
       - Current model (CurrentAttributes with user and session)
       - SessionsController (new, create, destroy)
       - Authentication concern
       - Migrations for users and sessions tables
       - Login views

    2. Review and customize the generated User migration:
       - Ensure `email` has a unique index
       - Add `email_verified_at:datetime` column (nullable, default nil) — used in Plan 02-02 for email verification gate
       - Ensure `password_digest` column is present (has_secure_password)

    3. Review and customize the generated Session migration:
       - Ensure sessions table has: user_id (foreign key with index), ip_address, user_agent, token (unique index)

    4. Customize User model:
       - Add `validates :email, presence: true, uniqueness: { case_sensitive: false }, format: { with: URI::MailTo::EMAIL_REGEXP }`
       - Add `validates :password, length: { minimum: 8 }, if: -> { password.present? || new_record? }`
       - Add `normalizes :email, with: ->(e) { e.strip.downcase }`
       - Add `has_many :sessions, dependent: :destroy`

    5. Create RegistrationsController:
       - `new` action: builds empty User, renders signup form
       - `create` action: creates User from strong params (email, password, password_confirmation). On success, redirect to login page with notice "Account created. Please check your email to verify your account." On failure, render :new with 422 status.
       - Strong params: `params.require(:user).permit(:email, :password, :password_confirmation)`

    6. Create app/views/registrations/new.html.erb:
       - Page title "Sign up" via content_for :title
       - Form with email field, password field, password_confirmation field, submit button
       - Display validation errors above form
       - Link to login page ("Already have an account? Sign in")
       - Use proper label elements for accessibility (WCAG 2.2 AA)

    7. Review generated app/views/sessions/new.html.erb:
       - Ensure it has email field, password field, submit button
       - Add link to signup page ("Don't have an account? Sign up")
       - Ensure proper label elements for accessibility

    8. Update config/routes.rb:
       - Keep existing root and health check routes
       - Add `resource :session` (singular) for login/logout (generated)
       - Add `resource :registration, only: [:new, :create]` for signup
       - Add `resources :passwords, param: :token` (generated — used in Plan 02-03)
       - Do NOT add before_action authentication yet (that's Plan 02-03)

    9. Run `bin/rails db:migrate` to apply migrations.

    10. Verify: `bin/rails routes | grep -E "session|registration|password"` shows expected routes.

    IMPORTANT: Do NOT add email verification gating to SessionsController yet — that's Plan 02-02. Users can log in immediately after registration in this plan. The email_verified_at column is added now but not enforced until 02-02.

    IMPORTANT: Do NOT add before_action :require_authentication to ApplicationController yet — that's Plan 02-03.

    IMPORTANT: The generated Authentication concern likely sets Current.session and Current.user. Review it and keep as-is unless it conflicts with the above requirements.
  </action>
  <verify>
    - `bin/rails db:migrate` exits 0
    - `bin/rails routes` shows session, registration, and password routes
    - `curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/session/new` returns 200 (login page)
    - `curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/registration/new` returns 200 (signup page)
    - Open signup form, create a user, verify redirect to login page
    - Open login form, log in with created user, verify session created
  </verify>
  <done>
    Signup form at /registration/new creates a User record with hashed password. Login form at /session/new authenticates and creates a Session. Logout destroys the session. User emails are unique and case-insensitive. Passwords require minimum 8 characters.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add fixtures, model tests, and integration tests for registration and login</name>
  <files>
    test/fixtures/users.yml
    test/fixtures/sessions.yml
    test/models/user_test.rb
    test/controllers/registrations_controller_test.rb
    test/controllers/sessions_controller_test.rb
  </files>
  <action>
    1. Create test/fixtures/users.yml with two users:
       - `verified_user`: email "alice@example.com", password_digest via BCrypt::Password.create("password123"), email_verified_at set to a past datetime
       - `unverified_user`: email "bob@example.com", password_digest via BCrypt::Password.create("password123"), email_verified_at nil
       Use ERB in fixtures: `password_digest: <%= BCrypt::Password.create("password123") %>`

    2. Create test/fixtures/sessions.yml with one session:
       - `alice_session`: user reference to verified_user fixture, ip_address "127.0.0.1", user_agent "Rails Test"

    3. Create test/models/user_test.rb:
       - Test email presence validation (blank email is invalid)
       - Test email uniqueness (duplicate email is invalid, case-insensitive)
       - Test email format validation (invalid format rejected)
       - Test email normalization (upcased email stored as lowercase)
       - Test password minimum length (< 8 chars rejected)
       - Test valid user creation with all fields

    4. Create test/controllers/registrations_controller_test.rb:
       - Test GET /registration/new returns 200 with signup form
       - Test POST /registration/new with valid params creates user and redirects
       - Test POST /registration/new with duplicate email returns 422
       - Test POST /registration/new with short password returns 422
       - Test POST /registration/new with mismatched password_confirmation returns 422

    5. Create test/controllers/sessions_controller_test.rb:
       - Test GET /session/new returns 200 with login form
       - Test POST /session with valid credentials creates session and redirects
       - Test POST /session with wrong password does not create session
       - Test POST /session with non-existent email does not create session
       - Test DELETE /session destroys session and redirects

    6. Run `bin/rails test` and ensure all tests pass (including existing Phase 1 tests).

    NOTE: Use the fixture password "password123" consistently. The sessions_controller_test should use the verified_user fixture for login tests (even though verification gating is not yet enforced — it will be in 02-02, and using verified_user now means tests won't break when gating is added).
  </action>
  <verify>
    - `bin/rails test` exits 0 with 0 failures, 0 errors
    - All new tests appear in output (model + controller tests)
    - Existing Phase 1 tests still pass
  </verify>
  <done>
    User model has 6+ tests covering validations and normalization. RegistrationsController has 5 integration tests. SessionsController has 5 integration tests. All pass alongside existing Phase 1 tests.
  </done>
</task>

</tasks>

<verification>
- `bin/rails test` passes with 0 failures (includes Phase 1 baseline + new auth tests)
- Signup form renders and creates users
- Login form authenticates valid users
- Logout destroys session
- User model validates email uniqueness, format, and password length
</verification>

<success_criteria>
A visitor can sign up with email and password, then log in with those credentials. Invalid signups (duplicate email, short password) are rejected with errors. Logout works. All tests pass green.
</success_criteria>

<output>
After completion, create `.ariadna_planning/phases/02-authentication/02-01-SUMMARY.md`
</output>
