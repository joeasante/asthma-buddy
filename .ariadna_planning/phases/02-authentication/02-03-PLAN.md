---
phase: 02-authentication
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - app/controllers/sessions_controller.rb
  - app/controllers/concerns/authentication.rb
  - app/controllers/application_controller.rb
  - app/controllers/passwords_controller.rb
  - app/mailers/passwords_mailer.rb
  - app/views/layouts/application.html.erb
  - app/views/sessions/new.html.erb
  - app/views/passwords/new.html.erb
  - app/views/passwords/edit.html.erb
  - config/routes.rb
  - test/controllers/passwords_controller_test.rb
  - test/controllers/sessions_controller_test.rb
  - test/system/authentication_test.rb
autonomous: true

must_haves:
  truths:
    - "A logged-in user remains authenticated after closing and reopening the browser"
    - "A user can click Sign out from any page and is immediately logged out"
    - "A user who forgot their password can request a reset link by email"
    - "A user can set a new password via the reset link"
    - "An unauthenticated visitor is redirected to the login page when accessing protected routes"
    - "Sign in, Sign up, and Sign out links appear in the navigation appropriately"
  artifacts:
    - path: "app/views/layouts/application.html.erb"
      provides: "Nav with conditional auth links"
      contains: "Sign out"
    - path: "app/controllers/application_controller.rb"
      provides: "Global authentication enforcement"
      contains: "require_authentication"
    - path: "app/views/passwords/new.html.erb"
      provides: "Password reset request form"
    - path: "app/views/passwords/edit.html.erb"
      provides: "New password form (from reset link)"
    - path: "test/system/authentication_test.rb"
      provides: "End-to-end system test for full auth flow"
  key_links:
    - from: "app/views/layouts/application.html.erb"
      to: "SessionsController#destroy"
      via: "Sign out link (button_to DELETE)"
      pattern: "button_to.*Sign out.*session_path.*delete"
    - from: "app/controllers/application_controller.rb"
      to: "app/controllers/concerns/authentication.rb"
      via: "include Authentication concern"
      pattern: "include Authentication"
    - from: "app/controllers/concerns/authentication.rb"
      to: "Current.session"
      via: "session cookie lookup"
      pattern: "Current\\.session"
    - from: "app/mailers/passwords_mailer.rb"
      to: "app/controllers/passwords_controller.rb"
      via: "reset link with signed token"
      pattern: "password.*token"
---

<objective>
Add persistent sessions (remember-me), password reset flow, sign-out link in navigation, and global route protection — then validate the entire auth system with a comprehensive system test.

Purpose: Completes AUTH-03 (persistent sessions), AUTH-04 (sign out from any page), AUTH-05 (password reset), and locks down all routes so unauthenticated visitors are redirected to login.
Output: Fully functional authentication system with persistent sessions, password reset, navigation auth links, route protection, and end-to-end system test.
</objective>

<execution_context>
@~/.claude/ariadna/workflows/execute-plan.md
@~/.claude/ariadna/templates/summary.md
</execution_context>

<context>
@.ariadna_planning/PROJECT.md
@.ariadna_planning/ROADMAP.md
@.ariadna_planning/phases/02-authentication/02-01-SUMMARY.md
@.ariadna_planning/phases/02-authentication/02-02-SUMMARY.md
@app/models/user.rb
@app/models/session.rb
@app/controllers/sessions_controller.rb
@app/controllers/concerns/authentication.rb
@app/controllers/application_controller.rb
@app/views/layouts/application.html.erb
@config/routes.rb
</context>

<tasks>

<task type="auto">
  <name>Task 1: Persistent sessions, password reset, nav auth links, and route protection</name>
  <files>
    app/controllers/sessions_controller.rb
    app/controllers/concerns/authentication.rb
    app/controllers/application_controller.rb
    app/controllers/passwords_controller.rb
    app/mailers/passwords_mailer.rb
    app/views/layouts/application.html.erb
    app/views/passwords/new.html.erb
    app/views/passwords/edit.html.erb
    app/views/sessions/new.html.erb
    config/routes.rb
  </files>
  <action>
    1. PERSISTENT SESSIONS — Make sessions survive browser close:
       - The Rails 8 auth generator uses a session cookie by default (expires when browser closes)
       - In the Authentication concern's `start_new_session_for(user)` method (or wherever the session cookie is set), change the cookie to be persistent:
         - Set `cookies.signed.permanent[:session_id] = { value: session.id, httponly: true, same_site: :lax }`
         - The `permanent` method sets a 20-year expiry, ensuring the session survives browser close
         - Alternatively, if the generator already uses a token-based approach, ensure the cookie has `expires: 2.weeks.from_now` instead of being a session cookie
       - Review what the generator created — it may already use a permanent cookie. If so, verify and adjust expiry to 2 weeks (not 20 years) for security:
         - `cookies.signed[:session_id] = { value: session.id, httponly: true, same_site: :lax, expires: 2.weeks.from_now }`

    2. PASSWORD RESET FLOW — The auth generator likely created PasswordsController and PasswordMailer. Review and customize:
       - PasswordsController (or PasswordResetsController — check what was generated):
         - `new` action: renders form asking for email address
         - `create` action: finds user by email, generates password reset token via `generates_token_for :password_reset, expires_in: 1.hour`, sends reset email, redirects to login with notice "If that email exists, we sent a reset link."
         - `edit` action: finds user by token, renders new password form. If token invalid/expired, redirect with alert.
         - `update` action: finds user by token, updates password, redirects to login with notice "Password reset. You can now sign in."
       - If the generator already created this, review for:
         - Token expiry (should be 1 hour, not longer)
         - Ambiguous response on create (don't reveal if email exists — always say "If that email exists...")
         - Proper form with password and password_confirmation fields

    3. Add `generates_token_for :password_reset, expires_in: 1.hour` to User model (if not already added by generator).

    4. Review/create password mailer:
       - PasswordsMailer (or whatever the generator named it) with `reset(user)` method
       - Email contains link to edit password page with token
       - Subject: "Reset your password — Asthma Buddy"

    5. Review/create password views:
       - app/views/passwords/new.html.erb: Email input form, submit button "Send reset link", link back to login
       - app/views/passwords/edit.html.erb: Password and password_confirmation fields, submit button "Reset password"
       - Ensure proper labels for accessibility

    6. Update app/views/sessions/new.html.erb:
       - Add "Forgot your password?" link pointing to new_password_path (or passwords_path for the form)

    7. NAV AUTH LINKS — Update app/views/layouts/application.html.erb nav section:
       - If user is logged in (check via helper or Current.session.present?):
         - Show user email as text
         - Show "Sign out" as `button_to "Sign out", session_path, method: :delete` (use button_to for DELETE, not link_to)
       - If user is NOT logged in:
         - Show "Sign in" link to new_session_path
         - Show "Sign up" link to new_registration_path
       - Use the Authentication concern's helper (likely `authenticated?` or check `Current.session`) — review what the generator provides

    8. ROUTE PROTECTION — Update ApplicationController:
       - Add `include Authentication` (if not already included by generator)
       - Add `before_action :require_authentication`
       - Add `skip_before_action :require_authentication` in:
         - HomeController (allow public homepage)
         - SessionsController (allow login page)
         - RegistrationsController (allow signup page)
         - EmailVerificationsController (allow verification link click)
         - PasswordsController (allow password reset flow)
       - The `require_authentication` method should redirect to new_session_path with alert "Please sign in to continue." if no current session
       - Review what the Authentication concern provides — it likely already has this method. Wire it up.

    9. Ensure config/routes.rb has all necessary routes (sessions, registrations, email_verifications, passwords). If password routes weren't added in 02-01, add them now.

    IMPORTANT: Review the generated Authentication concern carefully. It likely provides: `require_authentication`, `resume_session`, `start_new_session_for`, `terminate_session`. Use these methods rather than writing your own.

    IMPORTANT: The sign-out button MUST use `button_to` with `method: :delete` (not a plain link) for CSRF protection.

    IMPORTANT: For the nav, use a helper method or check `Current.session.present?` — do NOT check `session[:user_id]` directly. Use whatever pattern the generated Authentication concern establishes.
  </action>
  <verify>
    - `bin/rails routes` shows password routes (new, create, edit, update)
    - Session cookie has expiry set (not session-only) — check in browser dev tools or by inspecting the Set-Cookie header
    - Visiting a protected route while logged out redirects to /session/new
    - After login, nav shows email + "Sign out" button
    - While logged out, nav shows "Sign in" + "Sign up" links
    - "Forgot your password?" link on login page leads to reset form
    - Password reset form sends email (visible in logs in development)
  </verify>
  <done>
    Sessions persist across browser close (2-week cookie). Password reset works end-to-end with 1-hour token expiry. Nav shows conditional auth links. All routes require authentication except public pages.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integration tests for persistent sessions and password reset, plus full auth system test</name>
  <files>
    test/controllers/passwords_controller_test.rb
    test/controllers/sessions_controller_test.rb
    test/system/authentication_test.rb
    test/test_helper.rb
  </files>
  <action>
    1. Add a `sign_in(user)` helper method to test/test_helper.rb (if not already present from the generator):
       - Creates a session for the user
       - Sets the session cookie in the test client
       - Review what the generated Authentication concern provides for test helpers

    2. Create test/controllers/passwords_controller_test.rb:
       - Test GET /passwords/new returns 200 (reset request form)
       - Test POST /passwords with valid email enqueues reset email and redirects with ambiguous notice
       - Test POST /passwords with non-existent email still redirects with same notice (no information leak)
       - Test GET /passwords/:token/edit with valid token renders new password form
       - Test GET /passwords/:token/edit with invalid token redirects with error
       - Test PATCH /passwords/:token with valid token and valid password updates password and redirects
       - Test PATCH /passwords/:token with expired token redirects with error

    3. Update test/controllers/sessions_controller_test.rb:
       - Add test: after login, session cookie has future expiry (persistent, not session-only)
       - Add test: unauthenticated access to protected route (e.g., root if protected, or any future route) redirects to login

    4. Create test/system/authentication_test.rb — FULL end-to-end system test:
       - Test "complete auth journey":
         a. Visit signup page, fill in email + password + confirmation, submit
         b. Assert redirect to login with "check your email" notice
         c. Extract verification token from last delivered email (ActionMailer::Base.deliveries.last)
         d. Visit verification URL
         e. Assert "Email verified" notice
         f. Fill in login form, submit
         g. Assert logged in (nav shows email + Sign out)
         h. Navigate to homepage — still logged in
         i. Click "Sign out"
         j. Assert logged out (nav shows Sign in + Sign up)
         k. Visit login page, click "Forgot your password?"
         l. Fill in email, submit
         m. Extract reset token from delivered email
         n. Visit reset URL, enter new password
         o. Assert redirect to login
         p. Log in with new password — succeeds

    5. Run `bin/rails test` and `bin/rails test:system` — all must pass.

    NOTE: For extracting tokens from emails in system tests, use `ActionMailer::Base.deliveries.last.body.to_s` and parse the URL. Or use a regex to find the token URL in the email body.

    NOTE: System test uses headless Chrome (configured in Phase 1). The test must use Capybara fill_in / click_on / assert_text methods.
  </action>
  <verify>
    - `bin/rails test` exits 0 with 0 failures (unit + integration tests)
    - `bin/rails test:system` exits 0 with 0 failures (system test)
    - System test covers the complete journey: signup -> verify -> login -> navigate -> logout -> password reset -> login with new password
  </verify>
  <done>
    PasswordsController has 7 integration tests. SessionsController has persistent-session and route-protection tests. System test covers the complete auth journey end-to-end. All tests pass green alongside all prior tests.
  </done>
</task>

</tasks>

<verification>
- `bin/rails test` passes with 0 failures (all unit + integration tests)
- `bin/rails test:system` passes with 0 failures (auth system test)
- Complete auth flow works: signup -> verify email -> login (persistent) -> navigate -> logout -> password reset -> login with new password
- Unauthenticated visitors redirected to login
- Nav shows appropriate links based on auth state
</verification>

<success_criteria>
All five AUTH requirements are met:
- AUTH-01: User can create account with email and password (02-01 + 02-02)
- AUTH-02: User receives email verification after signup (02-02)
- AUTH-03: User can log in and stay logged in across browser sessions (persistent cookie)
- AUTH-04: User can log out from any page (nav sign-out button)
- AUTH-05: User can reset password via email link (password reset flow)
All tests pass green. System test validates the complete journey.
</success_criteria>

<output>
After completion, create `.ariadna_planning/phases/02-authentication/02-03-SUMMARY.md`
</output>
