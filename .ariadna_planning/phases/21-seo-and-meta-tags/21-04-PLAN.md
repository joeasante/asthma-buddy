---
phase: 21-seo-and-meta-tags
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - app/views/settings/show.html.erb
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "User can navigate to Medications from the Settings hub page"
    - "Settings hub shows a Medications card alongside the Profile card"
  artifacts:
    - path: "app/views/settings/show.html.erb"
      provides: "Settings hub with both Profile and Medications nav cards"
      contains: "settings_medications_path"
  key_links:
    - from: "app/views/settings/show.html.erb"
      to: "settings/medications#index"
      via: "settings_medications_path helper in anchor href"
      pattern: "settings_medications_path"
---

<objective>
Add a Medications nav card to the Settings hub page so users can reach their medications list directly from Settings.

Purpose: The Settings hub (/settings) only showed a Profile card. Medications was only reachable via the global nav — not from Settings. UAT test 5 failed because the user could not access medications through Settings. This single-card addition closes that gap.
Output: app/views/settings/show.html.erb with a second .section-card--nav linking to settings_medications_path.
</objective>

<execution_context>
@~/.claude/ariadna/workflows/execute-plan.md
@~/.claude/ariadna/templates/summary.md
</execution_context>

<context>
@.ariadna_planning/PROJECT.md
@.ariadna_planning/ROADMAP.md
@.ariadna_planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Medications nav card to settings-nav-grid</name>
  <files>app/views/settings/show.html.erb</files>
  <action>
In app/views/settings/show.html.erb, add a second nav card inside the `.settings-nav-grid` div, immediately after the closing `</a>` of the existing Profile card.

The new card must follow the identical `.section-card--nav` pattern as the Profile card:

```erb
<a href="<%= settings_medications_path %>" class="section-card section-card--nav" aria-label="Medications settings">
  <div class="settings-nav-icon">
    <svg width="24" height="24" viewBox="0 0 24 24" fill="none" aria-hidden="true" focusable="false">
      <path d="M9 3H15L17 7H7L9 3Z" stroke="currentColor" stroke-width="1.75" stroke-linejoin="round"/>
      <rect x="4" y="7" width="16" height="14" rx="1.5" stroke="currentColor" stroke-width="1.75"/>
      <line x1="12" y1="11" x2="12" y2="17" stroke="currentColor" stroke-width="1.75" stroke-linecap="round"/>
      <line x1="9" y1="14" x2="15" y2="14" stroke="currentColor" stroke-width="1.75" stroke-linecap="round"/>
    </svg>
  </div>
  <div class="settings-nav-body">
    <p class="settings-nav-title">Medications</p>
    <p class="settings-nav-description">Manage your inhalers, track doses, and monitor stock levels.</p>
  </div>
  <span class="settings-nav-chevron" aria-hidden="true"></span>
</a>
```

Do not alter the Profile card or any other content in the file.
  </action>
  <verify>
1. `bin/rails test` passes (all integration tests, no regressions).
2. Visit http://localhost:3000/settings while signed in — confirm two nav cards are visible: "Profile" and "Medications".
3. Click the Medications card — confirm it navigates to /settings/medications.
  </verify>
  <done>
Settings hub at /settings shows two nav cards. Clicking Medications navigates to /settings/medications. All existing tests pass with no regressions.
  </done>
</task>

</tasks>

<verification>
- `bin/rails test` exits 0 with no failures.
- /settings renders both the Profile card and the Medications card in the settings-nav-grid.
- The Medications card href resolves to /settings/medications.
</verification>

<success_criteria>
UAT test 5 gap closed: user can access medications from Settings by clicking the Medications card on the Settings hub page. No regressions in the 483-test suite.
</success_criteria>

<output>
After completion, create `.ariadna_planning/phases/21-seo-and-meta-tags/21-04-SUMMARY.md`
</output>
