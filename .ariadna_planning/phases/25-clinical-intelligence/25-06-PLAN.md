---
phase: 25-clinical-intelligence
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - app/assets/stylesheets/appointment_summary.css
  - app/assets/stylesheets/dashboard.css
  - app/views/appointment_summaries/show.html.erb
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Health report page has comfortable spacing between sections on screen"
    - "Zone legend has adequate padding and is not cramped"
    - "Notes cells wrap properly on mobile (no overflow)"
    - "Courses table does NOT show sick-day dose column"
    - "Reliever stat shows plain status label (Within range / Above range) with no jargon"
    - "Dashboard Health Report link is hidden on mobile"
    - "Health Report print button is hidden on mobile"
  artifacts:
    - path: "app/assets/stylesheets/appointment_summary.css"
      provides: "Screen spacing, mobile responsive rules, print button hide"
    - path: "app/assets/stylesheets/dashboard.css"
      provides: "Mobile hide rule for Health Report link"
    - path: "app/views/appointment_summaries/show.html.erb"
      provides: "Courses table without sick-day dose, simplified reliever label"
  key_links:
    - from: "appointment_summary.css"
      to: "show.html.erb"
      via: "CSS classes"
      pattern: "appt-summary|appt-section|appt-zone-legend"
---

<objective>
Fix 4 UAT issues from phase 25 user testing: cramped Health Report spacing, sick-day dose in courses table, jargon-y reliever label, and awkward mobile display of Health Report link and print button.

Purpose: Close all remaining UAT gaps so phase 25 can ship clean.
Output: 3 modified files (2 CSS, 1 ERB) — no controller changes.
</objective>

<execution_context>
@~/.claude/ariadna/workflows/execute-plan.md
@~/.claude/ariadna/templates/summary.md
</execution_context>

<context>
@.ariadna_planning/PROJECT.md
@.ariadna_planning/ROADMAP.md
@.ariadna_planning/phases/25-clinical-intelligence/25-UAT.md
@app/assets/stylesheets/appointment_summary.css
@app/assets/stylesheets/dashboard.css
@app/views/appointment_summaries/show.html.erb
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Health Report spacing, mobile responsive, reliever label, courses table, and mobile hides</name>
  <files>
    app/assets/stylesheets/appointment_summary.css
    app/assets/stylesheets/dashboard.css
    app/views/appointment_summaries/show.html.erb
  </files>
  <action>
    **appointment_summary.css — screen spacing fixes:**

    1. Increase `.appt-summary` gap from `var(--space-lg)` to `var(--space-xl)` — this is the main inter-section gap that was reported as cramped.

    2. Add padding to `.appt-zone-legend`: change `margin: var(--space-sm) 0 0` to `margin: var(--space-md) 0 0; padding: var(--space-sm) var(--space-md); background: var(--surface-alt); border-radius: var(--radius-md);` — gives the legend visual breathing room and a subtle background.

    3. Verify that the print-tightening CSS (the `.appt-summary { gap: 0.15cm; }` rule) is INSIDE the `@media print` block and not leaking to screen. It is already inside — confirm and leave it.

    4. Add a mobile responsive block for the detail tables. Add a `@media (max-width: 768px)` rule:
       - `.appt-notes-full, .appt-notes-cell { word-break: break-word; min-width: 80px; white-space: normal; max-width: none; }`
       - `.appt-detail-table { display: block; overflow-x: auto; -webkit-overflow-scrolling: touch; }`

    5. Hide the print button on mobile. Add inside the same `@media (max-width: 768px)` block:
       - `.page-header-actions .btn-secondary { display: none; }`

    **dashboard.css — hide Health Report link on mobile:**

    6. Add a `@media (max-width: 768px)` rule:
       - `.page-header-action-link { display: none; }`

    **show.html.erb — courses table fix:**

    7. In the "Courses during period" table (around line 215-231), remove the sick-day dose column. Remove the `<th>Sick day dose</th>` header and the corresponding `<td>` that renders `course.sick_day_dose_puffs`. The courses table should have columns: Name, Type, Dose, Dates.

    **show.html.erb — reliever label simplification:**

    8. Replace the "Guideline limit" stat (around line 166-168) with a plain status label. Change:
       - The `<dt>` from "Guideline limit" to just the status text itself — actually, restructure: replace the entire third `<div class="appt-stat">` with:
         ```erb
         <div class="appt-stat">
           <dt>Status</dt>
           <dd><%= @reliever_doses_per_week > 2 ? "Above range (>2/week)" : "Within range (<=2/week)" %></dd>
         </div>
         ```

    9. Remove the `<p class="appt-guideline-note">` line entirely (line 170). The status label is self-explanatory and the user said the extra text is not needed.
  </action>
  <verify>
    - Run `bin/rails test` to confirm no test regressions.
    - Visually inspect: `bin/rails server`, visit /health-report.
    - Confirm on desktop: sections have more breathing room, zone legend has background/padding, reliever section shows "Within range" or "Above range" with no "Guideline limit" label or explanatory note, courses table has no sick-day dose column.
    - Confirm on mobile (resize to < 768px): Health Report link gone from dashboard header, print button gone from Health Report header, notes cells wrap properly in detail tables.
    - Confirm print (Cmd+P): layout still tight for print (print CSS unchanged).
  </verify>
  <done>
    All 4 UAT issues resolved: (1) sections have comfortable screen spacing with zone legend visually distinct, (2) courses table excludes sick-day dose, (3) reliever shows "Within range/Above range" with no jargon or extra text, (4) dashboard link and print button hidden on mobile. Tests pass. Print layout unchanged.
  </done>
</task>

</tasks>

<verification>
- `bin/rails test` — all tests pass
- Desktop /health-report: increased section gaps, styled zone legend, simplified reliever label, courses table has 4 columns (no sick-day dose)
- Mobile /dashboard: no "Health Report" link visible in page header
- Mobile /health-report: no print button visible in page header
- Print preview: layout remains tight (print CSS unaffected)
</verification>

<success_criteria>
All 4 UAT gaps from 25-UAT.md are closed. No test regressions. Screen layout is comfortable. Print layout is unchanged.
</success_criteria>

<output>
After completion, create `.ariadna_planning/phases/25-clinical-intelligence/25-06-SUMMARY.md`
</output>
