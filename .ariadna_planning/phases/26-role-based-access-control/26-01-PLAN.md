---
phase: 26-role-based-access-control
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Gemfile
  - app/models/user.rb
  - app/policies/application_policy.rb
  - app/policies/symptom_log_policy.rb
  - app/policies/peak_flow_reading_policy.rb
  - app/policies/health_event_policy.rb
  - app/policies/medication_policy.rb
  - app/policies/dose_log_policy.rb
  - app/policies/notification_policy.rb
  - app/policies/profile_policy.rb
  - app/policies/user_policy.rb
  - app/policies/dashboard_policy.rb
  - app/controllers/application_controller.rb
  - app/controllers/admin/base_controller.rb
  - app/controllers/admin/users_controller.rb
  - app/controllers/admin/dashboard_controller.rb
  - app/controllers/symptom_logs_controller.rb
  - app/controllers/peak_flow_readings_controller.rb
  - app/controllers/health_events_controller.rb
  - app/controllers/notifications_controller.rb
  - app/controllers/profiles_controller.rb
  - app/controllers/dashboard_controller.rb
  - app/controllers/settings_controller.rb
  - app/controllers/settings/medications_controller.rb
  - app/controllers/settings/dose_logs_controller.rb
  - app/controllers/settings/accounts_controller.rb
  - app/controllers/preventer_history_controller.rb
  - app/controllers/reliever_usage_controller.rb
  - app/controllers/appointment_summaries_controller.rb
  - app/controllers/onboarding_controller.rb
  - db/migrate/TIMESTAMP_replace_admin_boolean_with_role_enum.rb
  - app/views/admin/users/index.html.erb
autonomous: true

must_haves:
  truths:
    - "User model has a role enum with at least :member and :admin values"
    - "Every controller action runs through a Pundit policy — verify_authorized or verify_policy_scoped is enforced"
    - "Accessing a resource without authorization raises Pundit::NotAuthorizedError which renders 403 or redirects with error"
    - "Existing admin panel works identically after migration from admin boolean to role enum"
    - "Admin users controller shows roles and allows changing between admin and member"
  artifacts:
    - path: "app/policies/application_policy.rb"
      provides: "Base Pundit policy with deny-by-default"
      contains: "class ApplicationPolicy"
    - path: "app/models/user.rb"
      provides: "User model with role enum"
      contains: "enum :role"
    - path: "db/migrate/*_replace_admin_boolean_with_role_enum.rb"
      provides: "Migration from admin boolean to role enum"
  key_links:
    - from: "app/controllers/application_controller.rb"
      to: "Pundit"
      via: "include Pundit::Authorization"
      pattern: "include Pundit::Authorization"
    - from: "app/controllers/application_controller.rb"
      to: "verify_authorized"
      via: "after_action"
      pattern: "after_action :verify_authorized"
    - from: "app/policies/*_policy.rb"
      to: "User#role"
      via: "user.admin?"
      pattern: "user\\.admin\\?"
---

<objective>
Replace the admin boolean with an extensible role enum, install Pundit, create policies for every controller, and enforce authorization across the entire application.

Purpose: Establish the authorization foundation that all SaaS features (API, billing, admin tools) depend on.
Output: Role-based User model, Pundit policies for all controllers, deny-by-default authorization enforcement.
</objective>

<execution_context>
@~/.claude/ariadna/workflows/execute-plan.md
@~/.claude/ariadna/templates/summary.md
</execution_context>

<context>
@.ariadna_planning/PROJECT.md
@.ariadna_planning/ROADMAP.md
@.ariadna_planning/STATE.md
@app/models/user.rb
@app/controllers/application_controller.rb
@app/controllers/concerns/authentication.rb
@app/controllers/admin/base_controller.rb
@app/controllers/admin/users_controller.rb
@app/controllers/admin/dashboard_controller.rb
@config/routes.rb
@app/views/admin/users/index.html.erb
</context>

<tasks>

<task type="auto">
  <name>Task 1: Role enum migration + Pundit gem + base policies + User model update</name>
  <files>
    Gemfile
    db/migrate/TIMESTAMP_replace_admin_boolean_with_role_enum.rb
    app/models/user.rb
    app/policies/application_policy.rb
    app/policies/symptom_log_policy.rb
    app/policies/peak_flow_reading_policy.rb
    app/policies/health_event_policy.rb
    app/policies/medication_policy.rb
    app/policies/dose_log_policy.rb
    app/policies/notification_policy.rb
    app/policies/profile_policy.rb
    app/policies/user_policy.rb
    app/policies/dashboard_policy.rb
    test/fixtures/users.yml
  </files>
  <action>
    1. Add `gem "pundit"` to Gemfile and run `bundle install`.

    2. Create migration `replace_admin_boolean_with_role_enum`:
       - Add `role` integer column with default 0 (member), null: false
       - Backfill: `UPDATE users SET role = 1 WHERE admin = true` (1 = admin)
       - Remove `admin` column
       - Add index on `role` column

    3. Update User model:
       - Add `enum :role, { member: 0, admin: 1 }, default: :member`
       - The enum provides `admin?`, `member?` methods which replaces the old boolean `admin?` — existing code calling `user.admin?` continues to work transparently.

    4. Run `bin/rails db:migrate`.

    5. Create `app/policies/application_policy.rb` (deny-by-default):
       - Default all actions to `false` (deny)
       - Provide `Scope` class that defaults to `scope.none`
       - Add helper method `admin?` that delegates to `user.admin?`

    6. Create individual policies:

       **SymptomLogPolicy**: All CRUD actions allowed for the record owner (`record.user == user`). Scope: `scope.where(user: user)`.
       **PeakFlowReadingPolicy**: Same owner-based pattern as SymptomLogPolicy.
       **HealthEventPolicy**: Same owner-based pattern.
       **MedicationPolicy**: Same owner-based pattern.
       **DoseLogPolicy**: Owner check via `record.medication.user == user`. Scope: `scope.joins(:medication).where(medications: { user_id: user.id })`.
       **NotificationPolicy**: Owner-based (`record.user == user`). `index?` and `mark_read?` and `mark_all_read?` allowed for owner.
       **ProfilePolicy**: `show?` and `update?` always true (users manage their own profile — the controller already scopes to Current.user).
       **UserPolicy**: `index?` admin only. `toggle_admin?` admin only (and record != user). `update_role?` admin only (and record != user). Scope: admin sees all users, members see none.
       **DashboardPolicy**: `index?` always true (all authenticated users can see their own dashboard).

    7. Update `test/fixtures/users.yml`: Replace `admin: true` with `role: 1` (or `role: admin`) for admin_user fixture. Remove any `admin: false` lines from other fixtures (default is member).
  </action>
  <verify>
    - `bin/rails db:migrate` succeeds
    - `bin/rails runner "puts User.first.role"` outputs "member" or "admin"
    - `bin/rails runner "puts User.roles"` outputs the enum hash
    - `bin/rails runner "u = User.new; puts u.admin?"` outputs false (default is member)
    - `ls app/policies/` shows all policy files
  </verify>
  <done>
    User model uses role enum (member/admin). Pundit gem installed. ApplicationPolicy denies by default. Individual policies exist for every resource controller. Old `admin?` method still works via enum.
  </done>
</task>

<task type="auto">
  <name>Task 2: Enforce Pundit authorization on every controller + update admin controllers</name>
  <files>
    app/controllers/application_controller.rb
    app/controllers/admin/base_controller.rb
    app/controllers/admin/users_controller.rb
    app/controllers/admin/dashboard_controller.rb
    app/controllers/symptom_logs_controller.rb
    app/controllers/peak_flow_readings_controller.rb
    app/controllers/health_events_controller.rb
    app/controllers/notifications_controller.rb
    app/controllers/profiles_controller.rb
    app/controllers/dashboard_controller.rb
    app/controllers/settings_controller.rb
    app/controllers/settings/medications_controller.rb
    app/controllers/settings/dose_logs_controller.rb
    app/controllers/settings/accounts_controller.rb
    app/controllers/preventer_history_controller.rb
    app/controllers/reliever_usage_controller.rb
    app/controllers/appointment_summaries_controller.rb
    app/controllers/onboarding_controller.rb
    app/controllers/home_controller.rb
    app/controllers/sessions_controller.rb
    app/controllers/registrations_controller.rb
    app/controllers/email_verifications_controller.rb
    app/controllers/passwords_controller.rb
    app/controllers/pages_controller.rb
    app/controllers/cookie_notices_controller.rb
    app/controllers/csp_reports_controller.rb
    app/views/admin/users/index.html.erb
  </files>
  <action>
    1. In `ApplicationController`:
       - Add `include Pundit::Authorization`
       - Add `after_action :verify_authorized, unless: :skip_authorization?`
       - Add `after_action :verify_policy_scoped, only: :index, unless: :skip_authorization?`
       - Add `rescue_from Pundit::NotAuthorizedError, with: :user_not_authorized`
       - Implement `user_not_authorized`: for HTML, redirect back with fallback to root and flash alert "You are not authorized to perform this action."; for JSON, render `{ error: "Forbidden" }` with status 403.
       - Implement `skip_authorization?` private method that returns true for controllers that don't need Pundit (unauthenticated controllers). The cleanest approach: define a class method `skip_pundit` in ApplicationController that sets an inheritable accessor, then call it in controllers that handle unauthenticated access.

    2. Controllers that skip Pundit entirely (unauthenticated access):
       - `SessionsController` — login/logout, no resource to authorize
       - `RegistrationsController` — signup, no authenticated user
       - `EmailVerificationsController` — email links, no authenticated user
       - `PasswordsController` — password reset, no authenticated user
       - `HomeController` — public landing page
       - `PagesController` — public static pages (privacy, terms, cookies)
       - `CookieNoticesController` — dismiss cookie banner
       - `CspReportsController` — inherits from ActionController::Base (not ApplicationController), so Pundit is not included at all; no changes needed
       - `ErrorsController` — error pages, no auth context

       For each of these, add `skip_after_action :verify_authorized` and `skip_after_action :verify_policy_scoped, raise: false` (or use the `skip_pundit` class method approach). These controllers handle unauthenticated access and have no resource to authorize.

    3. Resource controllers — add `authorize` calls:

       **SymptomLogsController**: `authorize @symptom_log` in show/edit/update/destroy; `authorize SymptomLog` in new/create; `policy_scope(SymptomLog)` in index (replace `Current.user.symptom_logs` with policy_scope — note: current code likely already scopes to `Current.user`, so policy_scope should produce the same result).

       **PeakFlowReadingsController**: Same pattern — authorize individual records, policy_scope for index.

       **HealthEventsController**: Same pattern.

       **Settings::MedicationsController**: Same owner-based pattern.

       **Settings::DoseLogsController**: Authorize through medication owner. `authorize @dose_log` for destroy; `authorize @medication, :show?` for index (user can see dose_logs if they own the medication); policy_scope for index.

       **NotificationsController**: `authorize Notification, :index?` for index; authorize individual notification for mark_read; `authorize Notification, :mark_all_read?` for mark_all_read.

       **ProfilesController**: `authorize :profile, :show?` and `authorize :profile, :update?` — since profile is Current.user, use a headless policy (ProfilePolicy with no record, or pass Current.user as record).

       **DashboardController**: `authorize :dashboard, :index?` — headless policy.

       **SettingsController**: `authorize :settings, :show?` — add a simple SettingsPolicy or use headless policy (all authenticated users can see settings). Create `app/policies/settings_policy.rb` if needed: `show?` returns true.

       **Settings::AccountsController**: `authorize :account, :destroy?` — all authenticated users can delete their own account. Add policy or headless.

       **PreventerHistoryController**: `authorize :preventer_history, :index?` — all authenticated users.

       **RelieverUsageController**: `authorize :reliever_usage, :index?` — all authenticated users.

       **AppointmentSummariesController**: `authorize :appointment_summary, :show?` — all authenticated users.

       **OnboardingController**: `authorize :onboarding, :show?` and `authorize :onboarding, :update?` — all authenticated users.

       For headless policies (no ActiveRecord model), use `Struct.new(:user, :record)` pattern or simply pass a symbol to `authorize`. Pundit supports `authorize [:admin, @user]` for namespaced policies and `authorize :dashboard` for headless policies — use the Struct-based approach where `record` is a Symbol.

    4. Admin controllers:
       - `Admin::BaseController`: Remove the manual `require_admin` before_action (Pundit handles this now). Keep the `require_admin` as a Pundit-based check: `authorize :admin_dashboard, :index?` — or better, let each admin action authorize its own resource.
       - `Admin::DashboardController#index`: `authorize :admin_dashboard, :index?` — admin only.
       - `Admin::UsersController#index`: `authorize User, :index?` (via UserPolicy#index? — admin only). Use `policy_scope(User)` for the query.
       - `Admin::UsersController#toggle_admin`: Find user, then `authorize @user, :toggle_admin?` (via UserPolicy).
       - Update `toggle_admin` action: Replace `user.admin?` boolean toggle with `user.admin? ? user.update!(role: :member) : user.update!(role: :admin)`. Replace `User.where(admin: true).count` with `User.admin.count` (enum scope).

    5. Update `app/views/admin/users/index.html.erb`:
       - Replace "Admin" column header with "Role"
       - Show role badge: `user.admin?` shows "Admin" badge, otherwise show "Member"
       - Replace button text: "Remove admin" / "Make admin" with "Change to Member" / "Change to Admin"
       - Update confirmation text accordingly
       - Keep the `toggle_admin_admin_user_path` route but rename to `update_role` if desired (keep backward compat for now — rename is optional)

    6. Update admin routes if the action name changed (optional — can keep `toggle_admin` name with role-based implementation).

    7. Run `bin/rails test` to check existing tests still pass (some may need updating due to Pundit enforcement — fix any that fail by adding proper authorization context).
  </action>
  <verify>
    - `bin/rails test` passes (existing tests updated as needed)
    - `bin/rails runner "puts ApplicationController.ancestors.include?(Pundit::Authorization)"` outputs true
    - Verify admin panel is accessible only by admin users (manual or via test)
    - Verify non-admin users get 403/redirect when accessing admin paths
  </verify>
  <done>
    Every controller action runs through Pundit authorization. verify_authorized safety net catches any missed authorize calls. Pundit::NotAuthorizedError renders 403 (JSON) or redirects with flash (HTML). Admin controllers use role enum instead of boolean. Admin users view shows roles.
  </done>
</task>

</tasks>

<verification>
- `bin/rails test` passes with no failures
- Admin user can access admin panel; member user gets redirected with error
- `bin/rails runner "User.find_by(email_address: 'admin@example.com').admin?"` returns true
- `bin/rails runner "User.find_by(email_address: 'admin@example.com').role"` returns "admin"
- No controller action can be called without Pundit authorization (verify_authorized catches it)
</verification>

<success_criteria>
- Role enum replaces admin boolean with zero-downtime migration
- Pundit policies exist for every controller with deny-by-default
- verify_authorized/verify_policy_scoped enforced on all actions
- Admin panel shows role column and role-change buttons
- Existing admin functionality works identically
- All tests pass
</success_criteria>

<output>
After completion, create `.ariadna_planning/phases/26-role-based-access-control/26-01-SUMMARY.md`
</output>
