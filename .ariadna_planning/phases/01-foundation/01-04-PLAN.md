---
phase: 01-foundation
plan: 04
type: execute
wave: 3
depends_on: [01-01, 01-02, 01-03]
files_modified:
  - config/deploy.yml
  - .kamal/secrets
  - .github/workflows/ci.yml
autonomous: false

user_setup:
  - service: kamal-registry
    why: "Kamal needs a Docker registry to push the container image to before deploying"
    env_vars:
      - name: KAMAL_REGISTRY_PASSWORD
        source: "Your Docker registry (e.g. Docker Hub: hub.docker.com -> Account Settings -> Security -> New Access Token; or GitHub: github.com -> Settings -> Developer settings -> Personal access tokens)"
    dashboard_config:
      - task: "Create a Docker registry repository named 'asthma-buddy'"
        location: "Docker Hub: hub.docker.com -> Create Repository OR GitHub Container Registry: ghcr.io (no manual creation needed)"
  - service: vps-server
    why: "Kamal deploys to a VPS — the server IP must be configured"
    env_vars: []
    dashboard_config:
      - task: "Provision a VPS (e.g., Hetzner CX21 — 2 vCPU, 4GB RAM, 40GB SSD) with Ubuntu 22.04 LTS"
        location: "Hetzner Cloud Console: console.hetzner.cloud"
      - task: "Note the server public IP address — needed for config/deploy.yml"
        location: "Hetzner Cloud Console -> Server detail page"
      - task: "Add your SSH public key to the server (Hetzner: add during provisioning or via SSH keys section)"
        location: "Hetzner Cloud Console -> SSH Keys"

must_haves:
  truths:
    - "config/deploy.yml has a real server IP, a real registry host, and a valid image name"
    - ".kamal/secrets pulls RAILS_MASTER_KEY from config/master.key and KAMAL_REGISTRY_PASSWORD from ENV"
    - "bin/kamal setup succeeds against the staging server without errors"
    - "CI pipeline runs bin/rails test and passes on push to main"
  artifacts:
    - path: "config/deploy.yml"
      provides: "Kamal deployment configuration with real server and registry values"
      contains: "servers:"
    - path: ".kamal/secrets"
      provides: "Kamal secrets file pulling from ENV and master.key"
      contains: "RAILS_MASTER_KEY"
    - path: ".github/workflows/ci.yml"
      provides: "CI pipeline with test, lint, and security scan jobs"
      contains: "bin/rails"
  key_links:
    - from: "config/deploy.yml"
      to: "VPS server"
      via: "SSH + Docker"
      pattern: "servers:"
    - from: ".kamal/secrets"
      to: "config/deploy.yml env.secret"
      via: "Kamal secret injection"
      pattern: "RAILS_MASTER_KEY"
---

<objective>
Configure Kamal with real staging server and registry details, verify the deployment pipeline works end-to-end with `bin/kamal setup`, and confirm the existing CI pipeline passes. This is the final gate for Phase 1.

Purpose: Phase success criterion 4 requires a Kamal deploy to succeed. The CI pipeline already exists in `.github/workflows/ci.yml` but needs a test run to confirm it passes. Kamal config needs real server and registry values replacing the placeholder `192.168.0.1` and `localhost:5555`.
Output: Working Kamal config for staging, confirmed CI green, deploy verification checkpoint.
</objective>

<execution_context>
@~/.claude/ariadna/workflows/execute-plan.md
@~/.claude/ariadna/templates/summary.md
</execution_context>

<context>
@/Users/josephasante/Code/asthma-buddy/.ariadna_planning/PROJECT.md
@/Users/josephasante/Code/asthma-buddy/config/deploy.yml
@/Users/josephasante/Code/asthma-buddy/.kamal/secrets
@/Users/josephasante/Code/asthma-buddy/.github/workflows/ci.yml
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>Staging server and Docker registry configuration for Kamal</decision>
  <context>
    The current config/deploy.yml has placeholder values:
    - Server IP: `192.168.0.1` (placeholder)
    - Registry: `localhost:5555` (placeholder)
    - Image: `asthma_buddy` (needs registry prefix)

    Kamal needs a real server IP and a real Docker registry before `bin/kamal setup` can run.
    The .kamal/secrets file needs KAMAL_REGISTRY_PASSWORD set in your environment.
  </context>
  <options>
    <option id="option-a">
      <name>Docker Hub + Hetzner VPS</name>
      <pros>Docker Hub is free for public images, Hetzner is cost-effective (CX21 ~€5/mo), both work well with Kamal</pros>
      <cons>Public image on Docker Hub unless you have a paid account</cons>
    </option>
    <option id="option-b">
      <name>GitHub Container Registry (ghcr.io) + Hetzner VPS</name>
      <pros>ghcr.io is free for private images with GitHub, no separate account needed</pros>
      <cons>Slightly more setup for GITHUB_TOKEN in secrets</cons>
    </option>
    <option id="option-c">
      <name>Skip staging deploy for now — CI only</name>
      <pros>Unblocks Phase 2 immediately without needing a server</pros>
      <cons>Phase 1 success criterion 4 (staging deploy) will not be met until a server is provisioned</cons>
    </option>
  </options>
  <resume-signal>
    Provide:
    1. Your chosen registry (Docker Hub username, ghcr.io username, or "skip staging")
    2. Your server IP (or "not provisioned yet")
    3. Confirm KAMAL_REGISTRY_PASSWORD is set in your shell ENV, or say "need help setting it up"

    Example: "Docker Hub: myusername, server: 65.21.100.42, registry password in ENV"
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Update Kamal config with real values and verify CI</name>
  <files>
    config/deploy.yml
    .kamal/secrets
    .github/workflows/ci.yml
  </files>
  <action>
    **This task runs AFTER the checkpoint decision above.**

    Using the values provided by the user:

    1. Update `config/deploy.yml`:
       - Replace `192.168.0.1` with the real server IP
       - Replace `localhost:5555` for `registry.server` with the real registry (e.g., `registry.hub.docker.com` for Docker Hub, or `ghcr.io` for GitHub)
       - Update `image:` to include the registry username prefix: e.g., `myusername/asthma-buddy` for Docker Hub or `ghcr.io/myusername/asthma-buddy` for ghcr.io
       - Add `username:` under `registry:` with the registry username
       - Add `KAMAL_REGISTRY_PASSWORD` to the `password:` list under `registry:`
       - Enable SSL proxy if using a domain (leave commented if IP-only for now)
       - Uncomment `config.assume_ssl = true` and `config.force_ssl = true` in `config/environments/production.rb` only if an SSL-terminating proxy is configured

    2. Update `.kamal/secrets` to pull KAMAL_REGISTRY_PASSWORD from ENV:
       ```bash
       KAMAL_REGISTRY_PASSWORD=$KAMAL_REGISTRY_PASSWORD
       RAILS_MASTER_KEY=$(cat config/master.key)
       ```

    3. Verify CI pipeline is correct — check `.github/workflows/ci.yml` has a `test` job that runs `bin/rails db:test:prepare test`. The existing file already has this. Confirm `checkout@v6` → update to `actions/checkout@v4` if v6 does not exist (GitHub Actions checkout latest stable is v4 as of early 2026).

    4. Run `bin/kamal setup` against the staging server. This:
       - Builds the Docker image
       - Pushes to registry
       - Pulls on server
       - Runs DB migrations
       - Starts the container

    If `bin/kamal setup` fails, diagnose the error and fix before marking done.
  </action>
  <verify>
    bin/kamal setup
    bin/kamal app details
    curl -s -o /dev/null -w "%{http_code}" http://{SERVER_IP}/up
  </verify>
  <done>
    - config/deploy.yml has real server IP and registry
    - bin/kamal setup exits 0
    - GET /up on the staging server returns 200
    - .github/workflows/ci.yml has correct checkout action version
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Kamal staging deployment — the app is running in a Docker container on the VPS, serving requests via Puma + Thruster, with Solid Queue running inside Puma.
  </what-built>
  <how-to-verify>
    1. Open a browser and visit `http://{YOUR_SERVER_IP}/` — you should see the "Asthma Buddy" homepage (the page from plan 01-02)
    2. Visit `http://{YOUR_SERVER_IP}/up` — should return a plain 200 OK response
    3. Check `bin/kamal app logs` for any startup errors
    4. On GitHub, push a commit to `main` and confirm the CI pipeline runs and all jobs (scan_ruby, scan_js, lint, test) pass green
  </how-to-verify>
  <resume-signal>Type "deployed" if staging is live and CI is green, or describe any issues encountered</resume-signal>
</task>

</tasks>

<verification>
1. `bin/kamal app details` — container running, no restart loops
2. `curl http://{SERVER_IP}/up` — returns 200
3. GitHub Actions CI pipeline: all jobs green on push to main
</verification>

<success_criteria>
- Staging deploy is live and returns 200 on root path and /up
- CI pipeline passes all jobs on push to main
- Kamal config uses real server IP and registry (no placeholder values)
- KAMAL_REGISTRY_PASSWORD loaded from ENV in .kamal/secrets (not hardcoded)
</success_criteria>

<output>
After completion, create `/Users/josephasante/Code/asthma-buddy/.ariadna_planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
