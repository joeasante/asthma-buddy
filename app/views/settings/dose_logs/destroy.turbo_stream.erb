<%# Replace the entire med row — remaining count, low_stock border, and history all update together %>
<%= turbo_stream.replace dom_id(@medication),
      partial: "settings/medications/medication",
      locals: { medication: @medication } %>

<%# Toast notification %>
<%= turbo_stream.replace "flash-messages" do %>
  <div id="flash-messages" data-controller="toast" data-toast-message="Dose removed." data-toast-variant="notice"></div>
<% end %>
