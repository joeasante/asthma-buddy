<% content_for :title, "Recovery Codes — Asthma Buddy" %>

<div class="page-header">
  <div class="page-header-eyebrow">
    <span class="eyebrow-section">Settings</span>
    <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true" style="opacity: 0.4; margin: 0 0.25rem;"><polyline points="9 18 15 12 9 6"/></svg>
    <span class="eyebrow-section">Security</span>
    <div class="eyebrow-rule" aria-hidden="true"></div>
  </div>
  <div class="page-header-main">
    <div class="page-header-left">
      <div class="page-header-icon" aria-hidden="true">
        <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <rect x="3" y="11" width="18" height="11" rx="2" ry="2"/>
          <path d="M7 11V7a5 5 0 0 1 10 0v4"/>
        </svg>
      </div>
      <div>
        <h1>Recovery Codes</h1>
        <p class="page-header-subtitle">Keep these codes safe — they can be used to access your account</p>
      </div>
    </div>
  </div>
</div>

<% if flash[:notice].present? %>
  <p role="status" class="flash flash--notice"><%= flash[:notice] %></p>
<% end %>

<div class="section-card">
  <div class="section-card-body">
    <div class="mfa-warning-card">
      <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
        <path d="M10.29 3.86L1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z"/>
        <line x1="12" y1="9" x2="12" y2="13"/>
        <line x1="12" y1="17" x2="12.01" y2="17"/>
      </svg>
      <p>Save these codes in a safe place. Each code can only be used once. If you lose access to your authenticator app, you can use a recovery code to sign in.</p>
    </div>

    <div class="mfa-recovery-codes-grid">
      <% @recovery_codes.each_with_index do |code, i| %>
        <div class="mfa-recovery-code">
          <span class="mfa-recovery-code-index"><%= (i + 1).to_s.rjust(2, "0") %></span>
          <code><%= code %></code>
        </div>
      <% end %>
    </div>

    <div class="form-actions">
      <%= form_with url: download_recovery_codes_settings_security_path, method: :post do |form| %>
        <%= form.submit "Download Codes", class: "btn-secondary" %>
      <% end %>
      <%= link_to "Done", settings_security_path, class: "btn-primary" %>
    </div>
  </div>
</div>
