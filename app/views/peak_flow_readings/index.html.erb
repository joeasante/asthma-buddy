<% content_for :title, "Peak Flow History" %>

<h1>Peak Flow History</h1>

<p>JSON API endpoint: <code>GET /peak-flow-readings.json</code></p>
<p>Parameters: <code>start_date</code> (default: 30 days ago), <code>end_date</code> (default: today)</p>
